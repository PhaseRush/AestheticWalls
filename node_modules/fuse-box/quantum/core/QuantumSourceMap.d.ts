import { File } from "../../core/File";
export declare class QuantumSourceMap {
    static generateOriginalSourceMap(file: File, generatedCode: string): Promise<any>;
    static generateSourceMap(code: any, name: any): string;
}
