interface Opts {
    originalCode: string;
    modify: (code: string) => string;
}
export declare class TestBlender {
    opts: Opts;
    private response;
    constructor(opts: Opts);
    static init(opts: Opts): Promise<TestBlender>;
    shouldMatchMapping(mapping: string): void;
    private run;
}
export {};
